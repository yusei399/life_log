<span>
	<%= link_to "投稿一覧", "/user_step_log"%>
</span>

<%= form_with model: @user_step_log, local: true do |f|%>
	<div class="field">
	<%= f.label :steps, "タイトル" %></br>
	<%= f.text_field :steps%>
	</div>
	<div class="submit">
		<%= f.submit "送信"%>
	</div>
<% end %>